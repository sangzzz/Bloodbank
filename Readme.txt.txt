Install nodejs with all the default settings.

Install mongodb with all the default settings.

open terminal
'cd' over to project folder,
run 'npm install', to install all the dependencies.

once complete, type 'node app.js' in the terminal.

Go to browser and go to "http://localhost:3000/"